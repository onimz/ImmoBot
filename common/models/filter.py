class Filter:
    def __init__(self,
                 id,
                 domain,
                 filter_url,
                 user_id):
        self.id = id
        self.domain = domain
        self.filter_url = filter_url
        self.user_id = user_id
